package syncgod.config;

/**
 * The name of the config values are directly coupled to the Settings Menu names, consider this
 * when changing or adding names.
 */

public enum ConfigValue {
    Fps,
    SkipFrameAmount,
    MouseResizeSens,
    WorkerRate,
    ClockRate,
    VideoWidth,
    VideoHeight
}