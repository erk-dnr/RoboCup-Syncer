/**
 * Implementation of publish subscribe pattern.
 * Topics can be registered to the {@link syncgod.pubsub.EventBus} and
 * classes can subscribe to each topic.
 * Subscriber get all events to a topic and can receive them with the related receive method.
 *
 * <p>
 *
 * @since 1.0
 */
package syncgod.pubsub;
